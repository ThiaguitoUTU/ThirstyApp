<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="0dp"
    android:gravity="top"
    android:background="@color/white"
    tools:context=".MainActivity4">

    <!-- Barra superior -->
    <View
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@color/light_blue" />

    <!-- Título en la parte superior -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tu peso"
        android:textSize="24sp"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:gravity="center"
        android:background="@color/light_blue"
        android:padding="10dp" />

    <!-- Contenedor central -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingTop="16dp">

        <!-- Imagen de la gotica -->
        <ImageView
            android:id="@+id/goticaImage"
            android:layout_width="112dp"
            android:layout_height="107dp"
            android:layout_gravity="center"
            android:contentDescription="gotica1"/>

        <!-- Layout para el muñeco y el NumberPicker -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:orientation="horizontal">

            <!-- Imagen del muñeco -->

            <!-- Contenedor del NumberPicker -->
            <ImageView
                android:id="@+id/munecoImage"
                android:layout_width="189dp"
                android:layout_height="352dp"
                android:layout_marginEnd="16dp"
                android:contentDescription="Elparcero"
                android:src="@drawable/hombred" />

            <LinearLayout
                android:layout_width="87dp"
                android:layout_height="267dp"
                android:gravity="center"
                android:orientation="vertical">

                <NumberPicker
                    android:id="@+id/numberPicker"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center" />

                <TextView
                    android:id="@+id/unitText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="10dp"
                    android:text="kg"
                    android:textSize="20sp"
                    android:textStyle="bold" />
            </LinearLayout>
        </LinearLayout>

        <!-- Botón continuar -->
        <Button
            android:id="@+id/botonContinuar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="24dp"
            android:backgroundTint="#007aff"
            android:text="CONTINUAR"
            android:textColor="#FFFFFF" />

    </LinearLayout>

</LinearLayout>